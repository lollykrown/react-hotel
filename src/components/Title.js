import React from 'react';

const Home = ({title}) => {
    return (
        <div className="section-title">
            <h4>{title}</h4>
            <div/>
        </div>
    )
}

export default Home;